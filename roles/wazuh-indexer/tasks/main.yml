---

- include_vars: ../../vars/main.yml

- include_vars: ../../vars/{{ urls_file }}

- import_tasks: dependencies.yml
  become: yes

- name: Set architecture facts
  block:
    - set_fact:
        host_architecture: "x86_64"
      when:
        - ansible_architecture == "x86_64"
        - ansible_os_family == "RedHat"
    - set_fact:
        host_architecture: "aarch64"
      when:
        - ansible_architecture == "aarch64"
        - ansible_os_family == "RedHat"
    - set_fact:
        host_architecture: "amd64"
      when:
        - ansible_architecture == "x86_64"
        - ansible_os_family == "Debian"
    - set_fact:
        host_architecture: "arm64"
      when:
        - ansible_architecture == "aarch64"
        - ansible_os_family == "Debian"

- name: Linux CentOS/RedHat | Install wazuh-indexer using yum
  yum:
    name: "{{ wazuh_indexer_package_download_path }}/{{ wazuh_indexer_package_name }}_{{ host_architecture }}.rpm"
    state: present
    disable_gpg_check: yes
  when:
    - ansible_os_family|lower == "redhat"

- name: Linux Debian | Install wazuh-indexer using dpkg
  shell: |
    dpkg -i {{ wazuh_indexer_package_download_path }}/{{ wazuh_indexer_package_name }}_{{ host_architecture }}.deb
  when:
    - ansible_os_family|lower != "redhat"

- import_tasks: "config_files_setup.yml"
  become: yes

- name: Reload systemd configuration
  systemd:
    daemon_reload: yes
  become: yes

- name: Ensure Wazuh indexer started and enabled
  service:
    name: wazuh-indexer
    enabled: true
    state: started

- name: Initialize Wazuh Indexer cluster
  shell: |
    /usr/share/wazuh-indexer/bin/indexer-security-init.sh
  args:
    executable: /bin/bash
  become: yes
  run_once: true

- name: Wait for Wazuh indexer API
  uri:
    url: "https://{{ hostvars[inventory_hostname].private_ip }}:9200/_cat/health/"
    user: "admin" # Default Indexer user is always "admin"
    password: "admin"
    validate_certs: no
    status_code: 200,401
    return_content: yes
    timeout: 4
  register: _result
  until:
    - _result is defined
    - '"green" in _result.content or ( "yellow" in _result.content and single_node )'
  retries: 24
  delay: 5

- name: Reload systemd configuration
  systemd:
    daemon_reload: true
  become: yes
  notify: restart wazuh-indexer

- name: Remove Wazuh Indexer installation leftovers
  file:
    path: "{{ wazuh_indexer_package_download_path }}"
    state: absent
  become: yes
